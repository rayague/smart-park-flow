"use client"

import { motion as m, AnimatePresence as AP } from "framer-motion"
import * as React from "react"

export const motion = m
export const AnimatePresence = AP
export const LazyMotion = m.div
